import React from 'react';
import { Card } from 'antd';

const KpiWidget = ({ state }) => {
  return (
    <Card title="Key Performance Indicators (KPI)" style={{ height: '100%' }}>
      <p>Widget content goes here...</p>
    </Card>
  );
};

export default KpiWidget;
