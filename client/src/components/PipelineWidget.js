import React from 'react';
import { Card } from 'antd';

const PipelineWidget = ({ state }) => {
  return (
    <Card title="Real-time Request Pipeline" style={{ height: '100%' }}>
      <p>Widget content goes here...</p>
    </Card>
  );
};

export default PipelineWidget;
